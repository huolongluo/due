# AI disclosure

Cursor (Grok 4.6) was used to implement the desk, Attestcoin worker, and DueVault. Invoice 4417, the HOLD / CITED / DUE stamps, and the rule that Reed cannot mint were specified and reviewed by the participant. The agent is not an unreviewed dump of generated code.

Replay underwrites with a local policy officer (`reed()`), not a vendor model. That is intentional: the AI track asks for models that act on cryptographically verified cross-chain data, not for shipping invoice amounts to a third party. If `GEMINI_API_KEY` is set later, it may only receive `opinion.saw`.
